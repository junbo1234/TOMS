<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <title>订单管理系统</title>
    <!-- 引入Bootstrap 5样式（兼容现有风格） -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- 自定义样式 -->
    <style>
        /* 左侧菜单固定 */
        .sidebar {
            width: 220px;
            height: 100vh;
            position: fixed;
            top: 0;
            left: 0;
            background-color: #f8f9fa;
            border-right: 1px solid #ddd;
            padding: 20px;
        }
        /* 右侧内容区（含标签栏） */
        .main-content {
            margin-left: 220px;
            padding: 20px;
        }
        /* 标签栏样式 */
        .tabs {
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
            margin-bottom: 20px;
        }
        .tab {
            display: inline-block;
            padding: 10px 20px;
            margin-right: 5px;
            border: 1px solid #ddd;
            border-bottom: none;
            border-radius: 5px 5px 0 0;
            cursor: pointer;
            background-color: #f1f1f1;
        }
        .tab.active {
            background-color: #fff;
            font-weight: bold;
        }
        .tab-close {
            margin-left: 10px;
            color: #999;
            cursor: pointer;
        }
        .tab-close:hover {
            color: #333;
        }
        /* 首页通知样式 */
        .notification-container {
            margin-top: 20px;
        }
        .notification-editor {
            width: 100%;
            height: 200px;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            resize: vertical;
        }
    </style>
</head>
<body>
    <!-- 左侧菜单 -->
    <div class="sidebar">
        <h4 class="mb-4">订单管理系统</h4>
        <ul class="list-unstyled">
            <li class="mb-2">
                <a href="{{ url_for('index_content') }}" class="menu-item text-decoration-none text-dark d-block p-2 rounded">首页</a>
            </li>
            <li class="mb-2">
                <a href="{{ url_for('order_download.html') }}" class="menu-item text-decoration-none text-dark d-block p-2 rounded">订单下载</a>
            </li>
            <li class="mb-2">
                <a href="{{ url_for('order_delivery.index') }}" class="menu-item text-decoration-none text-dark d-block p-2 rounded">销售订单发货</a>
            </li>
            <li class="mb-2">预留1</li>
            <li class="mb-2">预留2</li>
        </ul>
    </div>

    <!-- 右侧内容区（含标签栏） -->
    <div class="main-content">
        <!-- 标签栏 -->
        <div class="tabs"></div>

        <!-- 标签内容容器（动态加载页面） -->
        <div class="tab-content-container"></div>
    </div>

    <!-- 引入Bootstrap JS（可选，用于组件交互） -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <!-- 自定义交互逻辑 -->
    <script src="js/menu-tabs.js"></script>
</body>
</html>